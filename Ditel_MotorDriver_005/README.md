# オリジナルとの変更点
- PIDゲインが再設定できない不具合を修正
- PIDの操作量の上限を60000→10000に変更→その後1000まで落とす→400
- PIDの操作量の加減を0→1に変更
- 積分値の上限を積分ゲインによって可変するように変更(+オーバーフロー対策)
- 積分値の上限を5000→500に変更→200
- PIDゲイン設定時にスケールを変えられるようにした
- 最初のPIDゲインを0,0,0にした
- 制御周期10msを待機している時，CANのパケットを受け取れるようにした(今までは取りこぼしがあった)